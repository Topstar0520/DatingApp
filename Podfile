# Uncomment the next line to define a global platform for your project
platform :ios, '9.0'

target 'DatingApp' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for DatingApp
  
  # DEV removed both pod file because these are generating error while pod install
  # pod 'RestKit'
  # pod 'MulticastDelegate'
  
  pod 'MagicalRecord', :inhibit_warnings => true
  pod 'SDWebImage'
  pod 'Reachability'
  pod 'ECSlidingViewController' , :inhibit_warnings => true
  pod 'SDWebImage'
  pod 'DAKeyboardControl'
  pod 'MBProgressHUD'
  pod 'JASidePanels' , :inhibit_warnings => true
  pod 'CCBottomRefreshControl'
  pod 'TestFairy', :inhibit_warnings => true
  pod 'SIOSocket'
  pod 'FBSDKLoginKit'
  pod 'instagram-ios-sdk'
  pod 'Firebase/Core'
  pod 'Firebase/Messaging'
  pod 'GooglePlaces'
  pod 'HPGrowingTextView'
  # pod 'GoogleMaps', '~> 1.11.1'

end
